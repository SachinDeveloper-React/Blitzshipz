import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const CreateOrderScreen = (props: Props) => {
  return (
    <View>
      <Text>CreateOrderScreen</Text>
    </View>
  );
};

export default CreateOrderScreen;

const styles = StyleSheet.create({});
